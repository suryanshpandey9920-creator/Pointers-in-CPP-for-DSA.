# Pointers-in-CPP-for-DSA.
In this repo the ' POINTERS ' and the ' MEMORY ADDRESSING ', things are showed and also solved some basics level problems.
<br> <br>
Pointers in CPP inatilized with ( * pointerName ). <br>
<i> <u>ex : { 
    int  a = 23 ;
    int  *pointer = &a;

    cout << "value of a is : " << a  << endl ;
    cout << "value of  pointer is  : " << pointer << endl; }
<br> <br> 
Pointers contatil ( <u> datatype  * pointer name  =  & variable name) <br> <br> 

<i> Null pointers : The Null pointers does not store memory address of any variable and they also don't have any garbage value <i> <br> <br> 

</i> <u> Defrence operator the derefrence operator used to assign values in pass by refrence functions, they also print the actual value of a variable if the variable have any pointer the we just have to put a ( * ) symbol before the pointer name,<br> <br> ex: 

int a = 20;
int *pointer = &a ; <br>
cout << *pointer << endl; <br> <br>
this will print the actual value of the variable a. <br>